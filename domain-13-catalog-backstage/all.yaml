apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: domain-13-domain
  description: A collection of all Entities
spec:
  targets:
    - ./domain/domain-13-domain.yaml 
    - ./systems/*.yaml
    - ./components/*.yaml
    - ./apis/*.yaml
    - ./resources/*.yaml